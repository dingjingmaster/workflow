# AGENTS.project.md

> 项目特有 Agent 配置覆盖层。共享工作流可以跨项目同步，本文件用于保留当前项目长期有效的配置、安全边界和验证要求。

## 0. 加载规则

- 本文件必须与 `AGENTS.md` 一起加载；不存在时不阻塞启动。
- 本文件默认只读；只有用户明确指明加入、更新或删除 `AGENTS.project.md` 内容时，才允许创建或修改。
- 共享规则与本文件冲突时，执行更严格、更具体的规则；无法判断时暂停询问用户。
- 本文件只记录长期有效的项目事实、操作规则和安全边界，不记录临时任务细节。
- 可记录测试环境所需的敏感数据（如测试账号、测试密码、测试令牌或测试服务地址），但必须明确标注环境、用途、权限范围和轮换/废弃方式。
- 不记录生产环境、真实用户、个人账号、线上数据库、支付/云平台、私钥、私有证书正文、不可轮换密钥或权限超出测试环境的凭据。

## 1. 项目身份

- 项目名称：
- 仓库路径：
- 主要语言/框架：
- 运行环境：
- 关键负责人/维护者：

## 2. 项目安全边界

- 禁止触碰路径：
- 只读或生成文件：
- 需要用户确认的命令：
- 禁止执行的命令：
- 外部服务/生产环境边界：
- 敏感信息处理：
- 测试环境敏感数据：
  - 环境：
  - 用途：
  - 权限范围：
  - 轮换/废弃方式：

## 3. 项目固定规则

- 编码风格：
- 依赖管理：
- 分支/提交规则：
- 默认自动提交：否（如设为是，说明触发条件、提交范围和禁止提交内容）
- 文档更新规则：
- 其它长期规则：

## 4. 架构与领域约束

- 核心模块边界：
- 公共接口/协议：
- 数据模型/持久化：
- 兼容性要求：
- 性能或稳定性要求：

## 5. 验证契约

- 默认验证命令：
- 高风险改动必须执行：
- 无法验证时的记录要求：
- 发布/部署前检查：

## 6. 用户给出的长期规则

> 仅当用户明确要求加入或更新 `AGENTS.project.md` 时，才把“本项目以后都要遵守”“安全边界”“禁止触碰”“固定验证命令”等长期规则追加到本节。不要删除历史规则；规则废弃时标记状态并说明替代规则。

| 日期 | 规则/配置 | 适用范围 | 状态 | 来源 |
|------|-----------|----------|------|------|
| YYYY-MM-DD | [规则内容] | [范围] | 生效 | [用户说明/任务文档] |

## 7. 变更记录

| 日期 | 变更 | 原因 | 操作人/来源 |
|------|------|------|-------------|
| YYYY-MM-DD | 初始化项目覆盖层 | [原因] | [来源] |
